# v0.2.0 클라우드 마일스톤 셋업 가이드

이 문서는 **마님 Google 계정**으로 Firebase 프로젝트를 만들고, BudgetBook을 클라우드
공유 가계부로 전환하기 위한 단계별 안내입니다.

> **🔴 라이선스 · 보안 절대 원칙** (위반 시 회사 자산 정책 충돌 위험)
>
> - ✅ **개인 Gmail (`@gmail.com`)만 사용** — 마님도 본인도 무조건 개인 계정
> - ❌ **회사 Google Workspace 계정 절대 금지** (`@회사도메인.com`) — 회사 자산에 개인 가계부 데이터 묶이면 정책 위반
> - ❌ **회사 노트북·회사 네트워크에서 Firebase Console 작업 금지** — 개인 PC/폰/집 와이파이에서만
> - ✅ 가계부 = 개인정보 → Spark Plan 개인용 무료 한도 안에서 사용 (상업·기업용 아님)

> **🛠️ 운영 모드: "마님 owner + 본인 대리 작업"**
>
> - **owner**: 마님 Gmail (실제 주 사용자·관리자)
> - **셋업 작업자**: 본인(남편) — 마님 노트북/폰을 빌려서 마님 계정으로 로그인 후 Phase 1 30분 클릭 진행
> - **추가 안전장치**: 본인 Gmail을 **프로젝트 멤버 (Editor)로 함께 등록** → 보안 경고/청구 알림 영어 메일이 마님뿐 아니라 본인에게도 와서 본인이 1차 대응 가능. 마님이 영어 메일 받고 당황하는 일 방지.
> - **마님이 실제 하는 일**: ① iPhone에서 Google 로그인 1번 ② Safari "홈 화면에 추가" 1번 — 끝.

---

## 누가, 어디서, 무엇을

| 작업 | 누가 | 어디서 | 비고 |
|---|---|---|---|
| Firebase 프로젝트 생성 | 마님 (또는 마님 계정으로 대리 로그인) | 브라우저 (Console) | 마님이 owner |
| Authentication 설정 | 마님 | Console | Google 로그인 활성화 |
| Firestore DB 생성 | 마님 | Console | 서울 리전 |
| Hosting 활성화 | 마님 | Console | `.web.app` 무료 도메인 |
| 웹 앱 등록 + 키 받기 | 마님 | Console | firebaseConfig 복사 |
| **firebaseConfig 값 Claude에 전달** | 마님 (or 본인) | 이 채팅에 붙여넣기 | Phase 2 시작 신호 |
| 코드 통합 / 배포 / 마이그레이션 | Claude | Repo | 이후 전부 자동 |

---

## 전체 일정 (Phase)

| Phase | 내용 | 작업자 | 예상 |
|---|---|---|---|
| 1 | Firebase 프로젝트 생성 + 인증·DB·Hosting 활성화 | 마님 (Console) | 30분 |
| 2 | Firebase SDK 코드 통합 + 인증 흐름 | Claude | 4~5일 |
| 3 | 데이터 레이어 교체 (SQLite → Firestore) | Claude | 1주 |
| 4 | PWA 빌드 + 모바일 반응형 + Hosting 배포 | Claude | 4~5일 |
| 5 | 로컬 DB → Firestore 일회성 마이그레이션 + 실사용 검증 | Claude + 마님 | 며칠 |

---

## Phase 1 — Firebase Console 작업 (마님이 직접)

### 0. 사전 준비 (본인이 점검)

- ✅ **마님 개인 Gmail** (`@gmail.com`) — 회사 도메인 ❌
- ✅ **본인 개인 Gmail** (Phase 1.5에서 멤버로 추가할 용도)
- ✅ **마님 폰 옆에 두기** — 2단계 인증(2FA)이 켜져 있으면 마님 폰으로 인증코드가 옴 → 본인이 마님께 받아서 입력
- ✅ **개인 PC** + 집 와이파이 (회사 노트북·회사 네트워크 ❌)
- ✅ 약 30분의 여유 시간 (한 번에 쭉 진행 권장 — 30분 무활동 시 일부 화면 세션 만료)

> **2FA 켜져 있는지 확인하는 법**: 마님 폰에서 Gmail 앱 열고 우상단 프로필 → "Google 계정 관리" → "보안" 탭. "2단계 인증"이 "사용 중"이면 셋업 중 인증코드 입력 필요.

### 1. Firebase 프로젝트 생성

1. 브라우저에서 https://console.firebase.google.com 접속
2. **마님 Google 계정으로 로그인** (이미 로그인된 다른 계정이 있다면 우상단에서 전환)
3. 「프로젝트 만들기」 클릭
4. 프로젝트 이름 입력 — 권장: `budgetbook-family` 또는 `our-budget-book`
   - ⚠️ 이 이름이 무료 도메인 일부가 됨 → `https://budgetbook-family.web.app`
   - 영문 소문자 + 숫자 + 하이픈만 사용 가능
   - 너무 평범한 이름은 이미 누가 썼을 수 있어요 (자동으로 뒤에 숫자 붙음)
5. Google Analytics: **사용 안함** 선택 (가계부에 불필요, 무료 한도 절약)
6. 「프로젝트 만들기」 → 1분 대기 → 「계속」

### 2. Authentication 활성화 + Google 로그인 켜기

1. 좌측 메뉴 「Build → Authentication」
2. 「시작하기」 클릭
3. 「Sign-in method」 탭
4. 「Google」 행 클릭 → 「사용 설정」 토글 ON
5. **프로젝트 지원 이메일**: 마님 이메일 선택 (기본값 그대로)
6. 「저장」

### 3. 가족 구성원 사전 등록 (선택사항)

여기서 마님 + 본인(남편) Google 계정을 미리 등록해두면 첫 로그인 시 자동으로 통과됩니다.
**미등록 상태로 두고 로그인할 때 자동 등록되도록 하는 것도 OK** — Firestore 보안 규칙에서
허용된 이메일만 데이터 접근하도록 막을 거니까요.

만약 사전 등록을 원하면:
1. Authentication → 「Users」 탭 → 「사용자 추가」
2. 마님 이메일 + 임시 비밀번호 (Google 로그인이라 안 써도 무방)
3. 본인(남편) 이메일도 동일하게 추가

> **권장**: 이 단계는 건너뛰고, 다음 단계 모두 끝낸 다음에 두 사람 모두 한 번씩
> Google 로그인을 시도하면 자동으로 User 목록에 잡힙니다. 그게 더 깔끔해요.

### 4. Firestore Database 활성화

1. 좌측 메뉴 「Build → Firestore Database」
2. 「데이터베이스 만들기」 클릭
3. **위치**: `asia-northeast3 (서울)` 선택
   - ⚠️ 한 번 정하면 변경 불가. 한국 사용자 지연 최소화를 위해 서울 필수
4. **시작 모드**: 「테스트 모드로 시작」 선택
   - ⚠️ 30일 후 자동 잠금됨 — Phase 2에서 Claude가 보안 규칙 작성 후 적용할 거예요
5. 「사용 설정」 클릭

### 5. Hosting 활성화

1. 좌측 메뉴 「Build → Hosting」
2. 「시작하기」 클릭
3. 안내 화면 그냥 「다음 → 다음 → 콘솔로 이동」
   - CLI 설치 안내는 무시. Claude가 배포 자동화 설정할 거예요.

### 6. Web 앱 등록 + 설정 키 받기

1. 좌측 톱니바퀴(⚙️) → 「프로젝트 설정」 클릭
2. 「내 앱」 섹션에서 「`</>` (웹)」 아이콘 클릭
3. 앱 닉네임: `BudgetBook Web` (아무거나 OK)
4. **「Firebase Hosting 설정도 함께」 체크박스 ON** ← 중요
5. 「앱 등록」 클릭
6. 다음 화면에서 `firebaseConfig` 객체가 표시됨 — **그대로 복사**:
   ```js
   const firebaseConfig = {
     apiKey: "AIza...",
     authDomain: "budgetbook-family.firebaseapp.com",
     projectId: "budgetbook-family",
     storageBucket: "budgetbook-family.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456789:web:abc..."
   };
   ```
7. 메모장에 저장 → 이 채팅에 붙여넣기

### 7. 인증된 도메인 확인

1. Authentication → 「Settings」 탭 → 「Authorized domains」
2. 자동으로 `localhost`, `<프로젝트>.firebaseapp.com`, `<프로젝트>.web.app` 들어가 있을 것
3. 추가 등록 불필요 (커스텀 도메인 안 씀)

### 8. **[필수]** 본인 Gmail을 프로젝트 멤버로 추가

> ⚠️ 이 단계는 **운영 모드 "마님 owner + 본인 대리"의 핵심 안전장치**입니다. 건너뛰면
> 보안 경고 메일·청구 알림이 마님께만 가는데, 영어라서 당황할 수 있어요. 본인이
> Editor로 등록되면 같은 메일이 본인에게도 와서 1차 대응 가능.

1. 좌측 톱니바퀴(⚙️) → 「사용자 및 권한」 (또는 「프로젝트 설정」 → 「사용자 및 권한」 탭)
2. 「구성원 추가」 클릭
3. 본인 개인 Gmail 입력 (회사 Gmail ❌)
4. 역할: **Editor** 선택 (Owner는 마님만 유지)
5. 「구성원 추가」 → 완료
6. 본인 Gmail 받은편지함 확인 → "프로젝트 초대" 메일 → 「수락」 클릭

이제 본인도 Firebase Console에 본인 계정으로 직접 접근 가능하고, 보안 알림도 같이
받습니다.

---

## Phase 1 끝 — 마님 체크리스트

- [ ] 0. 사전 준비 — 개인 Gmail 2개 / 마님 폰 옆 / 개인 PC / 2FA 확인
- [ ] 1. Firebase 프로젝트 생성 (마님 개인 Gmail owner)
- [ ] 2. Authentication → Google 로그인 활성화
- [ ] 3. Firestore Database 생성 (서울 리전, 테스트 모드)
- [ ] 4. Hosting 활성화
- [ ] 5. 웹 앱 등록 + `firebaseConfig` 복사 완료
- [ ] 6. 인증된 도메인 자동 등록 확인
- [ ] 7. **[필수]** 본인 Gmail을 Editor로 프로젝트 멤버 등록 (보안 알림 공유)

이 6가지 끝나면 채팅에 `firebaseConfig` 6줄을 그대로 붙여넣어주세요.

> **보안 메모** — `apiKey`는 클라이언트 SDK 키라 공개돼도 보안 문제 없습니다 (Firestore
> Rules로 실제 보안을 합니다). 그래도 git에 안 올라가게 `.env` 파일로 분리해서 처리할
> 거예요. Phase 2에서 Claude가 자동 처리합니다.

---

## Phase 2 — 코드 통합 (Claude가 진행)

마님 firebaseConfig 받으면 Claude가:

1. ✅ `firebase` npm 패키지 설치 (`app/package.json`) — Phase 1 중 미리 완료
2. ✅ `app/.env.example` 작성 — Phase 1 중 미리 완료
3. ✅ `app/src/renderer/src/lib/firebase.ts` — Firebase 초기화 모듈 (스켈레톤)
4. ✅ `AuthContext` — React Context로 로그인 상태 관리 (스켈레톤)
5. ✅ `LoginView` — Google 버튼 1개짜리 단순 화면 (스켈레톤)
6. ✅ `App.tsx` — 로그인 게이트 (스켈레톤)
7. `firestore.rules` — **가족 구성원 화이트리스트** 기반 접근 제어:
   ```
   match /budgets/{doc} {
     allow read, write: if request.auth.token.email in [
       "마님이메일@gmail.com",
       "본인이메일@gmail.com"
     ];
   }
   ```
   (실제 이메일은 채팅에서 확정 후 적용)
8. `.env` 파일에 firebaseConfig 값 주입 (git ignore)

---

## Phase 3 — 데이터 레이어 교체

1. Firestore 컬렉션 스키마 설계 (transactions, categories, budgets, tags, accounts,
   recurring, savings_goals)
2. drizzle-orm 호출들을 Firestore SDK 호출로 교체
3. 기존 IPC 인터페이스(`window.api.*`)는 유지 — 내부 구현만 클라우드로
4. 트랜잭션 일관성 처리 (Firestore batch writes 활용)
5. 가족 공유 모델 — 모든 문서는 `familyId` 필드를 가지고, 마님 + 본인 두 UID 모두 같은
   `familyId` 그룹으로 묶임

---

## Phase 4 — PWA + 모바일 반응형 + 배포

1. `vite.config` PWA 플러그인 추가
2. `manifest.json` 작성 (앱 이름 "가계부", 아이콘, 색상)
3. Service worker 등록
4. 모바일 layout 분기 (sidebar → bottom tab, 표 → 카드, 모달 → 풀시트)
5. `firebase deploy --only hosting`로 첫 배포 → `https://<프로젝트>.web.app` 접속 가능
6. 마님 iPhone에서 Safari 열고 「공유 → 홈 화면에 추가」 — 앱처럼 실행됨
7. 본인 iPhone에서도 동일하게 설치 → 두 사람 동시에 같은 데이터 조회/수정 가능

---

## Phase 5 — 마이그레이션 + 실사용

1. v0.1.15 로컬 SQLite → Firestore 일회성 업로드 스크립트
2. Electron 데스크탑이 클라우드 우선으로 동작하도록 설정 토글
3. 1주일 실사용 후 피드백 반영
4. v0.1.15 데스크탑판은 "오프라인 백업용"으로 남겨두고, 일상 사용은 iPhone PWA 중심

---

## 무료 티어 한도 (Spark Plan, 신용카드 불필요)

- Firestore: 일 50K 읽기 / 20K 쓰기 / 20K 삭제 / 1GB 저장
- Auth: 무제한 사용자
- Hosting: 10GB 스토리지 / 일 360MB 다운로드
- 가족 2인 가계부 사용량은 한도의 1% 이하

**비용 발생 트리거**: Blaze 플랜(종량제) 명시 업그레이드를 **마님이 직접** 해야만 함.
자동 업그레이드 절대 없음 — 한도 초과 시 그냥 거절됨 (서비스 일시 중단). 가계부
데이터량 기준 초과 가능성 거의 0%.

---

## 비상 시 롤백

- v0.1.15는 `main` 브랜치에 그대로 보존 → 언제든 그 버전으로 재빌드 가능
- 로컬 SQLite 데이터는 마이그레이션 시 덮어쓰지 않음
- v0.2.0이 마음에 안 들면 v0.1.15로 돌아가서 로컬 사용 계속
- Firebase 프로젝트는 삭제하지 않고 그냥 안 쓰면 됨 (무료 한도라 비용 없음)

---

## 자주 묻는 질문 (FAQ)

### Q. 본인(남편)이 마님 계정으로 대리 작업해도 되나요?

가능합니다. 마님 노트북/폰에서 마님 계정으로 로그인해서 본인이 직접 클릭해도 결과
동일. 다만 2FA(2단계 인증)가 걸려있으면 마님 폰 인증 필요할 수 있어요.

### Q. 나중에 owner를 본인으로 옮길 수 있나요?

가능합니다. ⚙️ → 「사용자 및 권한」에서 본인 계정을 Owner로 추가하고 마님 Owner를
Editor로 강등하면 됨. 단 청구 계정(연결된 카드/한도)은 별도라 무료 티어 동안은 의미
없음.

### Q. 마님 Google 계정이 잠기면 어떻게 되나요?

- 데이터: 안전 (Firestore에 그대로 남음)
- 접근: 임시 차단됨. Google 계정 복구 후 정상화
- 백업: Phase 5의 일회성 마이그레이션이 끝나도 **로컬 SQLite는 그대로 둘 거**라 본인 PC에서 v0.1.15 실행 가능

### Q. 마님 + 본인 외 누군가 (예: 자녀) 추가하고 싶으면?

Phase 2의 Firestore Rules에 그 사람 Google 이메일을 추가하면 끝. 코드 한 줄 수정.
